import { frontendConstantsCommon } from './frontend-constants-common';

describe('frontendConstantsCommon', () => {
	it('should work', () => {
		expect(frontendConstantsCommon()).toEqual('frontend-constants-common');
	});
});
