import { backendConstantsCommon } from './backend-constants-common';

describe('backendConstantsCommon', () => {
	it('should work', () => {
		expect(backendConstantsCommon()).toEqual('backend-constants-common');
	});
});
